# Pomodoro

A Pomodoro timer for the desktop toolbar, themed after the class characters of Path of Exile 2, that
re-colors itself to match the active Omarchy theme.

Spec and design decisions live under `dev/` in the AI_Workspace-Blueprint pipeline
(`dev/active/self-pomodoro-poe2-timer/`) until the project stabilizes.
